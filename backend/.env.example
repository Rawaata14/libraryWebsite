# Server configuration
PORT=8000
FRONTEND_URL=http://localhost:5000
NODE_ENV=development

# Session configuration
# Replace this value with a long random secret in backend/.env
SESSION_SECRET=replace_with_a_secure_random_value

# MySQL configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=librarywebsite